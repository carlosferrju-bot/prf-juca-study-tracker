name: PRF JUCA Timer Checks
on: [push]
permissions:
  contents: read
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify timer source
        run: grep -q "startProgramTimer" index.html
