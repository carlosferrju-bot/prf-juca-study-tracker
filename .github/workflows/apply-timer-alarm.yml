name: Temporary PRF JUCA repair
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporary repair workflow disabled; cronograma fix is already applied."